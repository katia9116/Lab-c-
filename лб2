3.1. Як у C++ позначити степінь?
→ pow(x, n) 
3.2. Як у C++ позначити sin?
→ sin(x) 

3.3. Як у C++ позначити cos?
→ cos(x) 

3.4. Як у C++ позначити експоненту?
→ exp(x) 

Варіант 4

1 ЗАВДАННЯ

#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

int main() {
    double x;
    cout << "Введіть x: ";
    if(!(cin >> x)) {
        cout << "Невірний ввід.\n";
        return 1;
    }

    const double EPS = 1e-12;
    double Y;
    bool defined = true;

    if (x >= 0.0) {
        Y = exp(2.0 - x); // e^(2 - x)
    } else {
        if (fabs(x + 3.2) < EPS) {
            defined = false; // ділення на нуль у виразі 1/(x+3.2)
        } else {
            double t = 1.0 / (x + 3.2);
            Y = exp(-sin(t));
        }
    }

    cout << fixed << setprecision(9);
    if (defined) {
        cout << "Для x = " << x << " Y = " << Y << endl;
    } else {
        cout << "Для x = " << x << " вираз невизначений (ділення на нуль)." << endl;
    }

    return 0;
}






2 ЗАВДАННЯ


#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double x, y;
    cout << "Введіть координати точки (x, y): ";
    cin >> x >> y;

    bool inCircle = (x <= 0 && y <= 0 && (x*x + y*y <= 1));
    bool inTriangle = (x >= 0 && y >= 0 && y <= -x + 1);

    if (inCircle || inTriangle)
        cout << "Точка (" << x << ", " << y << ") належить заштрихованій області." << endl;
    else
        cout << "Точка (" << x << ", " << y << ") не належить заштрихованій області." << endl;

    return 0;
}
